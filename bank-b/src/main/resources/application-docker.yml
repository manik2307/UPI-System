spring:
  datasource:
    url: jdbc:mysql://bank-b-db:3306/bank_b
    username: root
    password: Manik@231896
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

server:
  port: 8082

logging:
  level:
    com.bank.b: DEBUG
